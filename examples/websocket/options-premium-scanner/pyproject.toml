[project]
name = "options-premium-scanner"
version = "0.1.0"
description = "A Python utility that streams real-time US options trades and filters them by premium size using Massive's WebSocket API"
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "Cole Power" }
]
license = { file = "LICENSE" }
dependencies = [
  "python-dotenv",
  "massive",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
